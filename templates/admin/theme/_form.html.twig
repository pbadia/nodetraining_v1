{{ form_start(form) }}

<div class="row">
    <div class="col-md-6">{{ form_row(form.name) }}</div>
</div>

<button class="btn btn-node">{{ button|default('Enregistrer') }}</button>

{{ form_end(form) }}